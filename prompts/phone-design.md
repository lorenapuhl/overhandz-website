1. Read `CLAUDE.md` and the skills from `@skills/`. Then, follow through with the tasks ahead. Ask me first, if you have any doubts. Only read the mentioned files (only read other files if truly necessary)

2. Make the following adaptations for phone widths:

- `HeroSection.tsx`: Center the text and CTA buttons. The Hero-photo is underneath and comes into view when scrolling. this is good.
- `FinalCTA.tsx`: center the CTA buttons
- Footer: Put "Train" section on the left and "Info" section on the right, next to "Train". right now, "info" is underneath "train".
- For computer screens AND phone screens : delete the "Case Study" link.
- In `AboutPageClient.tsx`: center all CTA buttons. (on computer screens, the first CTA buttons under the picture are not centered either. center them as well.
